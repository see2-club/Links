import { LoginTokenVerificationForm } from "@/components/auth/login-token-verification-form";

const LoginTokenVerificationPage = () => {
  return ( 
    <LoginTokenVerificationForm />
   );
}
 
export default LoginTokenVerificationPage;